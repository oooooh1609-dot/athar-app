# تقرير تدقيق ومراجعة — أثر (Athar)

التاريخ: 11 سبتمبر 2026
النطاق: كامل المستودع (216 ملفًا، ~24 ألف سطر)
الحالة: أُصلحت 13 مشكلة في هذه النسخة. القائمة الكاملة أدناه، مرتّبة حسب الخطورة.

> **تنبيه مهم:** لم يُنفَّذ `tsc` ولا `eslint` ولا `vite build` على هذه التعديلات، لأن البيئة التي أُجريت فيها المراجعة بلا اتصال بالشبكة ولا يمكنها تنزيل الحزم. كل تعديل روجع يدويًا سطرًا بسطر، لكن **يجب تشغيل `npm i && npm run lint && npm run build` قبل النشر**.

---

## 1. ملخص تنفيذي

المشروع ليس نموذجًا تجريبيًا — هو تطبيق TanStack Start + React 19 + Supabase مكتمل البنية، متخصص في تحسين صور النقوش وقراءة الخطوط العربية الجنوبية والشمالية القديمة. نقاط قوته الحقيقية:

- RLS مفعّل على كل الجداول الـ23 دون استثناء.
- الملحقات (extensions) تعمل داخل Worker معزول تُحجب فيه `fetch` و`indexedDB` و`importScripts` — تصميم صحيح وليس شكليًا.
- خدمة إعادة البناء ثلاثية الأبعاد تعتمد Meshroom ذاتي الاستضافة يستقصي عبر HTTPS صادر، فلا يُفتح منفذ وارد على جهاز المعالجة.
- الرسائل الموجّهة للنموذج في `analyze.ts` تمنع صراحةً رفع اليقين بسبب تلميح المُطابِق المحلي — انضباط علمي نادر في هذا النوع من التطبيقات.

لكن تحته ثغرة واحدة تُسقط كل ما سبق: **مفتاح توقيع الجلسات كان مشتقًا من ثابت مكتوب في الكود المصدري.**

---

## 2. الثغرات الأمنية

### 2.1 🔴 حرج — تزوير جلسة المدير من الكود المصدري

**الملف:** `src/lib/athar-auth.server.ts`

الكود السابق:

```ts
const secret =
  process.env["ATHAR_SESSION_SECRET"] || `athar-fallback-${DEFAULT_PIN_HASH.slice(-32)}`;
```

`DEFAULT_PIN_HASH` ثابت مكتوب في الملف نفسه. إذا لم يُضبط `ATHAR_SESSION_SECRET` — وهو غير موجود في ملف `.env` المرفق — فإن مفتاح HMAC يصبح قيمة يعرفها كل من لديه نسخة من المستودع. النتيجة: أي شخص يستطيع توليد

```
b64url({"role":"admin","seen":<الآن>}) + "." + HMAC
```

ويضعه في كوكي `athar_admin`، فيحصل على صلاحية كاملة في `/api/admin/access` و`/api/admin/extensions` و`/api/admin/worker` وكل ما عداها — دون معرفة كلمة المرور إطلاقًا.

**الإصلاح المطبّق:** حُذف الاحتياطي تمامًا. في الإنتاج يُرفع خطأ واضح إذا غاب `ATHAR_SESSION_SECRET` أو قلّ عن 32 حرفًا؛ وفي التطوير يُولَّد سرّ عشوائي لكل عملية تشغيل مع تحذير في السجل. كذلك `readToken` صار يرفض الطلب بهدوء (deny) بدل أن ينهار بخطأ 500 عند سوء الضبط.

**مطلوب منك:**

```bash
openssl rand -hex 32
```

وضعه في متغيّرات البيئة باسم `ATHAR_SESSION_SECRET` قبل أي نشر.

---

### 2.2 🔴 حرج — تجزئة كلمة مرور المدير منشورة في المستودع

**الملف:** `supabase/migrations/20260910121134_*.sql`

```sql
update public.admin_credentials
set password_hash = 'pbkdf2$120000$e9548609…$6137413a…'
```

التجزئة نفسها في تاريخ Git. PBKDF2-SHA256 بـ120 ألف تكرار يقاوم الكسر جيدًا **إذا كانت كلمة المرور قوية**. وبما أن الكود كان يقبل كلمات مرور من 4 أحرف (انظر 2.3)، فالاحتمال الراجح أنها قابلة للكسر دون اتصال في وقت قصير.

**لم يُصلَح تلقائيًا — إجراء يدوي مطلوب:** غيّر كلمة مرور المدير الآن إلى عبارة طويلة (16 حرفًا فأكثر). لا فائدة من حذف السطر من الملف؛ التاريخ يحتفظ به.

---

### 2.3 🟠 عالٍ — الحد الأدنى لكلمة مرور المدير كان 4 أحرف

**الملف:** `src/routes/api/admin/login.ts`

`if (next.length < 4)`. أربعة أحرف ≈ 1.7 مليون احتمال — لا شيء أمام كسر دون اتصال. رُفع إلى 12 حرفًا، مع حدّ أعلى 200، ومنع إعادة استخدام كلمة المرور الحالية.

---

### 2.4 🟠 عالٍ — كوكيز الجلسة بلا `Secure` في معظم البيئات

**الملف:** `src/lib/athar-auth.server.ts`

```ts
const secure = process.env["NODE_ENV"] === "production" ? "; Secure" : "";
```

منصّات serverless (Cloudflare Workers من ضمنها) كثيرًا ما تترك `NODE_ENV` غير مضبوط، فتُرسل كوكيز الجلسة بلا `Secure` وتنتقل عبر HTTP عادي. عُكس الشرط: `Secure` مفعّل دائمًا إلا إذا كان `NODE_ENV === "development"` صراحةً.

---

### 2.5 🟠 عالٍ — تعارض تزامن في استهلاك أكواد الدخول

**الملفات:** `src/lib/access.server.ts` + ترحيل جديد

المنطق السابق: اقرأ الصف → افحص `uses >= max_uses` في JavaScript → حدّث `uses + 1`. طلبان متزامنان يقرآن القيمة نفسها، ويجتازان الفحص كلاهما، فيدخل شخصان بكود استخدامه واحد.

**الإصلاح:** نُقل الفحص والزيادة إلى دالة `redeem_access_code` في PostgreSQL، حيث يُقفل الصف بـ`FOR UPDATE` فيتسلسل الطلبان. الترحيل الجديد:

```
supabase/migrations/20260911120000_atomic_access_code_redeem.sql
```

تشغيله مطلوب قبل نشر هذه النسخة، وإلا فشلت كل محاولات إدخال الأكواد.

---

### 2.6 🟠 عالٍ — الـworker يستطيع توجيه مهمة إلى نموذج مهمة أخرى

**الملف:** `src/lib/recon.server.ts`

`completeJob` كان يخزّن `modelPath` و`formats` كما وردا من الـworker، ثم يوقّعهما `signedModelUrl` لاحقًا دون تحقق. worker مخترق أو معطوب يستطيع كتابة مسار يخصّ مهمة عميل آخر، فتوقّعه نقطة الحالة وتسلّمه للمالك الخطأ.

**الإصلاح:** `pathBelongsToJob()` تُلزم كل مسار بأن يبدأ بـ`${jobId}/`، وتمنع `..` و`\` والمسارات المطلقة. المسارات المخالفة في `formats` تُسقَط بصمت، و`modelPath` المخالف يُفشل الطلب.

---

### 2.7 🟡 متوسط — نقطة التحليل بلا حدّ لحجم الطلب أو تحقق من نوع الصور

**الملف:** `src/routes/api/analyze.ts`

`z.string().min(32)` بلا حدّ أعلى، وبلا `content-length`، والقيمة تُمرَّر إلى المزوّد كـ`image_url`. مشكلتان:

1. جسم ضخم يُحتجز في الذاكرة ثم يُعاد إرساله إلى خدمة مدفوعة بالاستهلاك.
2. قيمة مثل `http://169.254.169.254/latest/meta-data/` ستجعل المزوّد يجلبها نيابةً عن المستخدم.

**الإصلاح:** حدّ 32 ميغابايت للجسم (بفحص `content-length` وبفحص فعلي بعد القراءة)، وحدّ 8 ميغابايت لكل صورة، وشرط أن تكون القيمة `data:image/(jpeg|png|webp);base64,` حصرًا.

---

### 2.8 🟡 متوسط — الجلسة "المنزلقة" لم تكن منزلقة

التوثيق أعلى `athar-auth.server.ts` يَعِد بـ«انتهاء بعد 30 دقيقة من الخمول»، لكن `seen` يُختَم مرة واحدة عند الإصدار ولا يُحدَّث. الأثر عملي: المدير يُطرد بعد 30 دقيقة من تسجيل الدخول مهما كان نشطًا.

**الإصلاح:** أُضيفت `slideToken()` التي تُعيد إصدار الرمز بعد انقضاء ثلث النافذة، ووُصلت بـ`GET /api/admin/login` التي تستدعيها واجهة الإدارة.

**نقص معروف:** الوصل الكامل يحتاج middleware يضيف الكوكي على كل استجابة محمية. حاليًا التجديد يحدث فقط عند فحص حالة تسجيل الدخول. الحل الصحيح موصوف في القسم 5.

---

### 2.9 🟡 متوسط — تحديد المعدّل غير فعّال ويتسرّب

`attempts` خريطة في الذاكرة داخل isolate واحد. على منصة توزّع الطلبات على عدة isolates تتضاعف المحاولات المسموحة بعددها. كما أن الخريطة لم تكن تُنظَّف أبدًا.

**الإصلاح الجزئي:** أُضيف تنظيف دوري وسقف 5000 عميل مُتتبَّع، مع توثيق صريح للقيد. **الحل الحقيقي** يحتاج حالة مشتركة (جدول Supabase أو Durable Object) — انظر القسم 5.

كذلك `clientKey` يعيد `"unknown"` عند غياب رؤوس الـIP، فيتشارك كل هؤلاء دلوًا واحدًا.

---

### 2.10 🟠 عالٍ — ملف `.env` لم يكن مستثنى من Git

`.gitignore` لم يذكر `.env` إطلاقًا، والملف موجود في المستودع ويحتوي `SUPABASE_URL` و`SUPABASE_PROJECT_ID` و`SUPABASE_PUBLISHABLE_KEY`. المفتاح المنشور هو publishable (محمي بـRLS، والخطر محدود)، لكن القاعدة خاطئة من أساسها: أول مفتاح خدمة (`SUPABASE_SERVICE_ROLE_KEY`) يُضاف إلى الملف كان سيُرفع معه — وهو مفتاح يتجاوز RLS بالكامل.

**الإصلاح:** أُضيف `.env` و`.env.*` إلى `.gitignore` مع استثناء `.env.example`، وأُنشئ `.env.example` موثّق يشرح كل متغيّر مطلوب.

**إجراء يدوي:** إن كان `.env` قد رُفع فعلًا إلى مستودع بعيد، دوّر مفاتيح Supabase. (لا تُعِد كتابة تاريخ Git — المشروع مربوط بـLovable، وإعادة الكتابة تُتلف تاريخه هناك؛ انظر `AGENTS.md`.)

---

## 3. تسريبات الذاكرة والأداء

14 استدعاءً لـ`URL.createObjectURL` مقابل 4 استدعاءات `revokeObjectURL` فقط.

### 3.1 🟠 الأسوأ — معاينة ورشة العمل

**الملف:** `src/components/athar/Workspace.tsx`

`runPreview()` تُستدعى بعد كل حركة لأي شريط تمرير (debounce 350ms)، وكانت تُنشئ صورتَي blob بالحجم الكامل وتستبدل الرابطين السابقين دون تحرير. جلسة ضبط واحدة من عشرين حركة تترك أربعين صورة محتجزة في الذاكرة حتى إغلاق الصفحة — على الهاتف هذا سبب انهيار التبويب.

**الإصلاح:** تحرير الرابط السابق داخل `setBeforeUrl`/`setAfterUrl` عبر دالة التحديث.

### 3.2 🟡 روابط التنزيل

`Workspace.tsx` (موضعان) و`Projects.tsx` كانت تبني `<a href=createObjectURL>` وتنقر عليه دون تحرير.

**الإصلاح:** ملف جديد `src/lib/object-url.ts` فيه `downloadBlob()` تُرفق العنصر بالمستند، تنقر، تزيله، ثم تُحرّر الرابط بعد 60 ثانية (التحرير الفوري يُلغي التنزيل في Safari).

### 3.3 🟡 صور القوائم

`Projects.tsx` و`routes/index.tsx` كانا يستدعيان `createObjectURL` داخل جسم المكوّن — رابط جديد في **كل** إعادة رسم، ولا تحرير لأيٍّ منها.

**الإصلاح:** خطّاف `useObjectUrl()` في `object-url.ts`، ومكوّنان فرعيان `ProjectImage` و`RecentThumb` (الخطّافات لا تُستدعى داخل `.map`).

### 3.4 🟡 مصغّرات التصوير ثلاثي الأبعاد

`Capture3D.tsx` يُنشئ رابطًا لكل لقطة ولا يحرّره عند الحذف ولا عند مغادرة الشاشة. جلسة تصوير من 80 صورة تُبقيها كلها حيّة.

**الإصلاح:** تحرير فوري عند حذف اللقطة، وتنظيف شامل عند الـunmount عبر `shotsRef`.

---

## 4. أخطاء التخزين المحلي

**الملف:** `src/lib/athar-db.ts`

### 4.1 🟠 تسريب اتصال IndexedDB

```ts
t.oncomplete = () => db.close();
```

الإغلاق عند الاكتمال فقط. أي معاملة تفشل أو تُجهَض تترك اتصالًا حيًا، والاتصالات الحيّة **تحجب ترقية نسخة قاعدة البيانات** — أي أن أي ترقية مستقبلية للمخطط كانت ستتعطّل عند بعض المستخدمين دون سبب ظاهر.

**الإصلاح:** `try/finally` يُغلق على كل المسارات، مع معالجة `onabort` و`onblocked` وفحص وجود `indexedDB` أصلًا (مهم للـSSR).

### 4.2 🟡 الشاشة الرئيسية تقرأ كل المشاريع لعرض ثلاثة

```ts
setRecent((await listProjects()).slice(0, 3));
```

`getAll()` يجلب كل السجلات بكل صورها ولقطاتها، ثم يُرمى 97% منها. يتكرر عند كل دخول لتبويب الرئيسية.

**الإصلاح:** رُفعت نسخة القاعدة إلى 3 مع فهرس `createdAt`، وأُضيفت `recentProjects(limit)` التي تمشي بمؤشّر عكسي وتلمس السجلات المطلوبة فقط.

---

## 5. ما لم يُصلَح — يحتاج قرارك

| # | الموضوع | لماذا لم يُصلَح |
|---|---|---|
| 1 | تجديد الجلسة على كل نقطة محمية | يتطلب تغيير توقيع `requireAdminRequest` وتعديل عشرات جمل `Response.json` المتفرّقة. التعديل الآمن يحتاج مُصرِّفًا للتحقق. |
| 2 | تحديد معدّل مشترك بين الـisolates | قرار معماري: جدول Supabase (أبسط) أم Durable Object (أدق). |
| 3 | اسم النموذج `openai/gpt-6-astra` في `analyze.ts` | لم أستطع التحقق من صحته دون اتصال. راجعه مقابل توثيق البوابة. |
| 4 | `src/integrations/supabase/types.ts` | الملف مُولَّد آليًا، وأضفتُ فيه تعريف `redeem_access_code` يدويًا. إعادة التوليد بعد تشغيل الترحيل ستُنتج التعريف نفسه. |
| 5 | 35 استخدامًا لـ`as any` / `: any` | تحتاج مراجعة حالة بحالة. |

---

## 6. الفجوة بين المواصفات والكود

وثيقة «أثر - رفيق الآثار» التي أرفقتَها تصف تطبيقًا أوسع كثيرًا من الموجود. هذه ليست أخطاء، بل عمل لم يبدأ بعد:

| في الوثيقة | الحالة في الكود |
|---|---|
| تحسين الصور بالذكاء الاصطناعي | ✅ موجود ومتقدّم (CLAHE، decorrelation stretch) |
| قراءة النقوش والمساعد الذكي | ✅ موجود، مع طبقة معجم ومقارنات منشورة |
| تحويل الصور إلى 3D | ✅ موجود (Meshroom ذاتي الاستضافة) |
| إدارة المشاريع والحماية | ✅ موجود |
| **التصوير 360° والتجميع (stitching)** | ❌ غير موجود |
| **الواقع المعزّز والقياس (AR Measurement)** | ❌ غير موجود |
| **الخرائط وإحداثيات GPS** | ❌ غير موجود |
| **تقارير PDF** | ⚠️ `pdfjs-dist` موجود للقراءة فقط، لا للتوليد |
| **وضع Offline / QR / VR** | ❌ غير موجود |
| React Native / Expo | ❌ المشروع ويب (يمكن تغليفه كـPWA) |

**ملاحظة على الأولويات:** الوثيقة تفترض تطبيقًا أصليًا بـReact Native، بينما الموجود تطبيق ويب. تحويله يعني إعادة كتابة كاملة، لا إضافة. الأقرب عمليًا: تحويله إلى PWA وإضافة GPS (عبر `navigator.geolocation`) وتوليد PDF أولًا — كلاهما ممكن في الويب دون تغيير المعمار. أما AR فيحتاج WebXR، وهو محدود الدعم على iOS.

---

## 7. قائمة ما يجب فعله قبل النشر

1. `openssl rand -hex 32` → `ATHAR_SESSION_SECRET`.
2. غيّر كلمة مرور المدير (التجزئة الحالية منشورة في Git).
3. شغّل الترحيل الجديد `20260911120000_atomic_access_code_redeem.sql`.
4. `npm i && npm run lint && npm run build` — تعديلات هذه النسخة لم تُصرَّف.
5. أعد توليد `types.ts` من Supabase بعد الترحيل.
6. اختبر يدويًا: إدخال كود دخول، تصدير صورة محسّنة، حذف لقطة 3D، دخول المدير ثم انتظار 25 دقيقة والتأكد من بقاء الجلسة.
7. انسخ `.env.example` إلى `.env` واملأه. إن كان `.env` القديم قد وصل إلى مستودع بعيد، دوّر مفاتيح Supabase.
