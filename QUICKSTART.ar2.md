# تشغيل «أثر» على جهازك — دليل سريع

ثلاث طرق، من الأسرع إلى الأكمل. ابدأ بالأولى.

---

## الطريقة ١ — تجربة محلية بلا قاعدة بيانات (٥ دقائق)

هذه أسرع طريقة لترى التطبيق يعمل. **لا تحتاج Supabase ولا أي حساب.**

### ما تحتاجه

[Node.js 20 أو أحدث](https://nodejs.org) فقط. تحقّق:

```bash
node --version
```

### الخطوات

```bash
# ١. فُك الضغط وادخل المجلد
unzip athar-v4.zip -d athar
cd athar

# ٢. ثبّت الحزم (يحتاج إنترنت، مرة واحدة، ~٣ دقائق)
npm install

# ٣. جهّز الإعدادات
cp .env.example .env
```

افتح `.env` في أي محرر، وغيّر سطرًا واحدًا فقط:

```
ATHAR_LOCAL_MODE="1"
```

ثم:

```bash
# ٤. شغّل
npm run dev
```

افتح **http://localhost:3000**

### ما الذي يعمل في هذا الوضع

| يعمل ✅ | لا يعمل ❌ |
|---|---|
| الكاميرا والتصوير | قراءة النقوش (تحتاج مفتاح AI — الطريقة ٢) |
| تحسين الصور (CLAHE، decorrelation stretch) | إعادة البناء ثلاثي الأبعاد (تحتاج خادم Meshroom) |
| القياس بمرجع المقياس | لوحة الإدارة |
| عارض ٣٦٠° ونقاط المعلومات | |
| لوحات مفاتيح الخطوط القديمة | المكتبة المرجعية |
| المشاريع والحفظ على الجهاز | |
| تسجيل الموقع وتصدير GeoJSON | |
| تقارير PDF | |
| البحث الذكي | |

> **تنبيه أمني:** `ATHAR_LOCAL_MODE` يفتح بوابة الدخول. يعمل فقط خارج الإنتاج وفقط عندما لا توجد قاعدة بيانات متصلة. **لا تضعه أبدًا على خادم منشور.**

---

## الطريقة ٢ — مع الذكاء الاصطناعي (١٠ دقائق)

قراءة النقوش موجودة في التطبيق منذ البداية — الناقص هو المفتاح فقط. أضفتُ الآن دعم ثلاثة مزوّدين، اختر واحدًا.

### الخيار الأسهل: OpenAI مباشرة

احصل على مفتاح من [platform.openai.com/api-keys](https://platform.openai.com/api-keys) ثم في `.env`:

```
OPENAI_API_KEY="sk-..."
ATHAR_AI_MODEL="gpt-4o"
```

### خيار أرخص: OpenRouter

```
ATHAR_AI_BASE_URL="https://openrouter.ai/api/v1"
ATHAR_AI_API_KEY="sk-or-..."
ATHAR_AI_MODEL="google/gemini-2.0-flash-001"
```

### خيار مجاني تمامًا: Ollama محليًا

```bash
# ثبّت من ollama.com ثم:
ollama pull llama3.2-vision
```

```
ATHAR_AI_BASE_URL="http://localhost:11434/v1"
ATHAR_AI_API_KEY="ollama"
ATHAR_AI_MODEL="llama3.2-vision"
```

> **شرط واحد لا تنازل عنه:** النموذج **يجب** أن يدعم الرؤية (vision). التطبيق يرسل صورًا، فنموذج نصي فقط سيفشل مهما كان جيدًا. جودة القراءة تتفاوت كثيرًا بين النماذج — Gemini وGPT-4o أفضل بوضوح من النماذج المحلية الصغيرة في النقوش.

أعد التشغيل بعد أي تعديل على `.env`:

```bash
npm run dev
```

---

## الطريقة ٣ — النشر الكامل

تحتاج هذه: مشروع Supabase، وترحيلات قاعدة البيانات، ومفتاح جلسة، وخادم Meshroom للتصوير ثلاثي الأبعاد.

```bash
# ١. مفتاح الجلسة (إلزامي في الإنتاج)
openssl rand -hex 32
```

ضع الناتج في `ATHAR_SESSION_SECRET`، واملأ قيم Supabase الستّ في `.env`.

```bash
# ٢. شغّل كل الترحيلات في محرر SQL بلوحة Supabase
#    من supabase/migrations/ بالترتيب الزمني
#    ولا تنسَ الترحيل الجديد:
#    20260911120000_atomic_access_code_redeem.sql

# ٣. ابنِ وشغّل
npm run build
npm run preview
```

ثم من لوحة الإدارة: **غيّر كلمة مرور المدير فورًا** — التجزئة الحالية منشورة في تاريخ Git وقابلة للكسر.

للنشر عبر Lovable: ادفع الكود إلى Git وسيلتقطه تلقائيًا.

---

## اختبار العمل دون اتصال

عامل الخدمة لا يعمل في وضع `dev` (يحتاج HTTPS أو بناءً كاملًا):

```bash
npm run build
npm run preview
```

ثم في Chrome اضغط F12:

1. **Application → Service Workers** — تأكد من `activated`
2. **Application → Manifest** — يجب أن يظهر زر التثبيت بلا تحذيرات
3. **Network → Offline** ثم أعد التحميل — يظهر شريط «لا يوجد اتصال»
4. جرّب تحسين صورة وقياسها وحفظها **وأنت دون اتصال** — يجب أن تنجح كلها

### التثبيت على الهاتف

- **Android / Chrome:** القائمة ⋮ ← «تثبيت التطبيق»
- **iPhone / Safari:** زر المشاركة ← «إضافة إلى الشاشة الرئيسية»

> iOS يتطلّب HTTPS حقيقيًا. للتجربة على الهاتف من جهازك، استخدم نفقًا مثل `npx localtunnel --port 3000`.

---

## تشغيل الفحوص

**١٧٧ فحصًا**، بلا أي تثبيت — Node وحده. كلها دفعة واحدة:

```bash
node docs/run-tests.mjs
```

وفحص اتساق يمرّ على ١٨٠ ملفًا: كل استيراد يُحَل، وكل اسم مستورد موجود فعلًا، وكل مفتاح ترجمة مُعرَّف في اللغات الأربع:

```bash
node docs/consistency-check.mjs
```

أو واحدًا واحدًا:

```bash
node docs/measure.test.mjs   # ٢٣ — حسابات القياس وانتشار الخطأ
node docs/geo.test.mjs       #  ٧ — الإحداثيات والمسافات
node docs/sw.test.mjs        # ٢٠ — قواعد التخزين المؤقت
node docs/outbox.test.mjs    # ١٢ — الطابور والتراجع الأسّي
node docs/search.test.mjs    # ٢٦ — تطبيع العربية والنقحرة
node docs/sites.test.mjs     # ٢٣ — تجميع المواقع وGeoJSON
node docs/ai.test.mjs        # ٣٢ — اختيار المزوّد وبناء الطلب
node docs/panorama.test.mjs  # ٣٤ — إسقاط ٣٦٠° والنقاط
```

---

## مشكلات شائعة

| العَرَض | السبب والحل |
|---|---|
| `Missing Supabase environment variable(s)` | لم تضع `ATHAR_LOCAL_MODE="1"`، أو وضعت مفتاح Supabase جزئيًا. في الوضع المحلي اترك كل قيم Supabase فارغة. |
| شاشة إدخال كود لا تتجاوزها | نفس السبب أعلاه. |
| `No AI service is configured` | لم تضع أي مفتاح AI. راجع الطريقة ٢. |
| القراءة تفشل بـ 404 | اسم النموذج في `ATHAR_AI_MODEL` غير موجود لدى المزوّد. |
| القراءة تفشل بـ 400 | النموذج لا يدعم الرؤية. بدّله بنموذج vision. |
| الكاميرا لا تفتح | المتصفحات تمنع الكاميرا خارج HTTPS، ما عدا `localhost`. |
| تعديل `.env` بلا أثر | أعد تشغيل الخادم؛ لا يُعاد تحميله تلقائيًا. |
| عامل خدمة عالق | في وحدة التحكم: `import("/src/lib/pwa").then(m => m.unregisterAll()).then(() => location.reload())` |

---

## ماذا بعد التجربة

اقرأ `docs/AUDIT.md` قبل أي نشر حقيقي — فيه ١٣ عيبًا أُصلح وثلاثة إجراءات يدوية ما زالت مطلوبة منك.
و`docs/ARCHITECTURE.md` فيه ما بُني وما لم يُبنَ ولماذا.
